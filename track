Frontend Tickets — List a Track
F1. Route shell + entry points
waynook.com/tracks browse page (placeholder list if B12 not ready)
"List your track" CTA in nav (logged-in only) → /tracks/new
Profile: "My listings" tab → /host/listings
Route guard: unauthenticated → login with redirect back
Acceptance: deep links work; CTA hidden for logged-out users
F2. Listing wizard shell
5-step layout: Basics / Location / Details & Photos / Availability & Contact / Review
Stepper UI with validation state per step
Draft autosave: save on every step change + 30s debounce; "Save & exit" button
Resume draft: /tracks/:id/edit reopens at furthest valid step
Mobile-first; sticky footer with Back / Next / Save
Acceptance: refresh mid-wizard loses nothing (server draft)
F3. Step 1 — Basics form
Fields: name*, tagline, description* (char counter), type* (select)
Client validation: required, length limits
Acceptance: matches API field names; errors map to fields from API 422 responses
F4. Step 2 — Location
Country/region/city selects (cascading) + address line
Map picker (Leaflet/Mapbox): draggable pin, geocode on address blur
"Show approximate area only" toggle with explainer tooltip
Acceptance: pin syncs with address both ways; hidden-address preview shown
F5. Step 3 — Details & photos
Numeric fields: length, turns; selects: surface, direction
Facilities multi-select chips
Rules textarea
Photo uploader: multi-file, drag-drop, progress, client-side compression, min 3 validation, reorder (drag), cover selection (first by default)
Optional video URL field with preview
Acceptance: upload failures retry per-file; max 15 enforced
F6. Step 4 — Availability & contact
Weekly hours editor (per-day enable + time range)
Closed-date exceptions date picker
Pricing: amount + currency + unit (hour/day/session)
Contact mode radio (in-app only / phone after approval / email) + response-time select
Phase 1 cut version: hours as simple text/JSONB, per-day price only — decide with B9 scope
Acceptance: invalid time ranges blocked client-side
F7. Step 5 — Review & submit
Full public-preview render of the listing
Authorization checkbox + host-terms checkbox (required)
Submit → success screen: "Under review" (or "Live" for trusted hosts) with link to My listings
Validation-error summary if API rejects (map errors back to steps)
Acceptance: double-submit prevented; success state correct per listing status
F8. Host dashboard — My listings
Table/cards: cover thumb, name, status badge, submitted date
Actions per status: edit (draft/rejected), view (active), resubmit (rejected), delete (with confirm)
Rejected state shows admin reason
Acceptance: status badges match backend statuses exactly
F9. Admin moderation queue
List of PENDING_REVIEW + flagged listings, oldest first
Detail view: full listing + photo viewer + approve / reject (reason required) / flag
Address-needs-review filter (from B8 fallback)
Admin route guard
Acceptance: actions reflect in host's dashboard within one refresh
F10. Notifications UI (minimal)
Toast/email-link landing: rejected listing deep-links to edit page with reason banner
Phase 1: no in-app inbox
Acceptance: deep links from emails land correctly
Dependency order
F1 → F2 → F3 → F4 → F5 → F7 (core flow shippable) F6 after B9 scope decided; F8 after B4; F9 after B4/B8; F10 last

Phase 1 cut
Merge F3–F5 into one long form (no stepper) if you want it live fast; F9 can be a plain admin table
