fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
email-validator>=2.0
httpx>=0.27
pytest>=8.0

# Optional: Firebase Auth (server verifies Firebase ID tokens).
# The app runs fine without it - auth falls back to local JWT.
firebase-admin>=6.5
